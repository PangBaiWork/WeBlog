<?xml version="1.0" encoding="utf-8"?>


<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/main_item"
        android:title="Project">
        <menu>


            <item
                android:id="@+id/project_server"
                android:icon="@drawable/ic_server"
                android:title="@string/live_preview"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/project_script"
                android:icon="@drawable/ic_script"
                android:title="@string/script_manager"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/project_close"
                android:icon="@drawable/ic_close"
                android:title="@string/close_project"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>
    <item
        android:id="@+id/labels_item"
        android:title="Global">
        <menu>
            <item
                android:id="@+id/global_env"
                android:icon="@drawable/ic_environment"
                android:title="@string/environment"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/global_setting"
                android:icon="@drawable/ic_setting"
                android:title="@string/setting"
                android:checkable="true"
                app:showAsAction="ifRoom"/>

        </menu>
    </item>
</menu>
